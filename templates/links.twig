<ul class="documented-links-list">
    {% for name, url in config.links %}
        <li class="documented-link-item">
            <a href="{{ url }}" target="_blank">{{ name }}</a>
        </li>
    {% endfor %}
</ul>
